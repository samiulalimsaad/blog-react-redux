import React from "react";

const Navbar = () => {
    return (
        <nav className="bg-slate-100 shadow-md">
            <div className="max-w-7xl mx-auto px-5 lg:px-0 flex justify-between py-3 items-center text-3xl font-semibold">
                <p>Blog</p>
            </div>
        </nav>
    );
};

export default Navbar;
